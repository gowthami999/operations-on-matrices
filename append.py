import numpy as np
x=np.array(input("enter the list values"))
y=np.array(input("enter the another list values which has to append"))
z=np.append(x,[y])
print('the given values are:',x)
print('the new are are which has to append:',y)
print('after appending the new values are:',z)

